# Shared Contracts

Cross-service schemas and examples used by backend and frontend.

## Contents

- `schemas/`: JSON Schemas for request/response payloads
- `examples/`: example payloads for tests and fixtures
- `contracts/`: API contract placeholders (OpenAPI or notes)

Recommended next step: generate TypeScript types and Python models from a single source of truth.

